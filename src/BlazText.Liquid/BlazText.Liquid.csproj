<Project Sdk="Microsoft.NET.Sdk.Razor">

  <PropertyGroup>
    <Description>Shopify Liquid templating plugin for the BlazText editor (powered by Fluid): supply drops for live preview, get autocomplete for drop names, and detect which drops a template actually uses.</Description>
  </PropertyGroup>

  <ItemGroup>
    <SupportedPlatform Include="browser" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.Web" Version="10.0.8" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\BlazText\BlazText.csproj" />
    <ProjectReference Include="..\BlazText.Rendering\BlazText.Rendering.csproj" />
  </ItemGroup>

</Project>
