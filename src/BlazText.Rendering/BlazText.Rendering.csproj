<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Rendering pipeline for BlazText documents: Liquid templating (Fluid), embedded image resolution, and email-safe CSS inlining (PreMailer.Net). Blazor-free, so the same pipeline runs in the editor preview and on your backend.</Description>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\BlazText.Models\BlazText.Models.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Fluid.Core" Version="2.31.0" />
    <PackageReference Include="PreMailer.Net" Version="2.7.3" />
  </ItemGroup>

</Project>
